# 交接 — B2B AI 客服平台（2026-09-20 夜）

给接手会话：**这份是自包含的，你没看过之前的对话，请先读完再动手。**
仓库：`D:/360Downloads/360驱动大师目录/b2b-ai-support-plan/b2b-ai-support-plan`

**本文档对"项目状态"是权威的。** 另有 `HANDOVER-2026-09-19.md`（另一会话的，
记录客户聊天界面那条线，仍然有效）和 `.workbuddy-ai/agent-handover-prompt.md`
（我上一轮的交接，**已过期，应删除**——留两份互相矛盾的交接正是本项目反复出现的陷阱）。

---

## 0. 先读这些（按顺序，约 25 分钟）

1. `AGENTS.md` —— 仓库铁律（10 条架构规则 + 完成定义 + 禁止的走捷径）。**最高约束。**
2. `.workbuddy-ai/memory/MEMORY.md` —— 长期项目记忆。**最省时间的一份。**
3. `.workbuddy-ai/memory/REFERENCE.md` —— 长尾细节（Windows 陷阱、浏览器验收方法）。
4. `.workbuddy-ai/memory/2026-09-20.md` —— 本轮工作日志，含所有判断依据**和错误**。
5. `docs/research/huaqiu-research.md` —— **项目计划本体**（阶段 0–4、指标、风险登记册）+ 附录 A–J。
6. `docs/research/chinese-intent-measurement.md` —— 中文意图的实测报告（很多"缺口"在这里已被量过）。
7. `docs/acceptance/` —— 另一会话的 round 0–8 验收记录；`07-cross-review.md` 是缺口清单
   （**注意它是快照，会过期，动手前重新测量**）。

**不要删 `.workbuddy-ai/`**，它是项目数据不是缓存。

---

## 1. 当前状态（全部为**实测**，不是记忆）

| 项 | 值 |
|---|---|
| 全量测试 | **1582 收集 / 0 失败 / 0 错误 / 0 跳过** |
| ruff / ruff format / mypy | 全绿（329 文件 / 142 源文件） |
| `release_check --evidence-only` | exit 0，零容忍计数 15 / 4 / 3 |
| 分支 | `master`，**领先 `origin/master` 18 个提交（未 push）** |
| 远端 | **已存在**：`git@github.com:kayon0209/b2b-ai-support-platform.git`（旧交接说"没有远端"，已过时） |
| 未提交 | **18 个文件**（3 新增 + 15 修改），全是我本轮的 |
| 迁移 | head `0040_case_attachments`，`EXPECTED_MIGRATIONS = 40` |

**18 个未提交文件里混着另一会话对 `HANDOVER-2026-09-19.md` 的 4 行补充。
不要整体 commit，也不要 reset——先问。**

---

## 2. 本轮（2026-09-20）做了什么

1. **阶段 3 · 证据附件**（迁移 `0040` + `cases/attachments.py` + `POST/GET /v1/cases/{id}/attachments`）。
   上传走 multipart、读取走**预签名 URL**；`FORCE` RLS；对象键不可穿越（有变异验证）。
   15 条测试。
2. **修掉一个分层错误**：`MinioStorage.put_object` 自己按 **knowledge 的白名单**校验，
   导致共享基础设施只能存 knowledge 认得的东西。校验移到 `upload_object`，
   `_storage` 提升为公开的 `object_storage`（唯一构造点）。
3. **中文场景词表**：`_SCENE_PATTERNS` 八组原本**全是英文**，
   所以**任何中文消息都拿不到场景**。已补中文备选（`\b(?:en)\b|(?:cn)`），
   九条中文用例现在都有场景，英文侧未动，评估套件仍全绿。
4. **前端 sourcemap**：构建改 `sourcemap: "hidden"`（bundle 不再声明 map，已实测为 0），
   并把"部署排除 `*.map`"写进上线清单（**明确标注是部署期检查、无自动化门禁**）。
5. **删掉我自己上一轮留下的死代码** `CaseCategory.QUALITY_COMPLAINT`（无消费者）。

---

## 3. 未完成的事，按价值排序（含实测的阻塞原因）

### 🔴 3.1 质量投诉**没有转人工**（最高价值，且未被阻塞）

计划多处写明：投诉是 **L6，必须转人工**，且
**"AI 绝不可做归责表态或赔付承诺"**（报告 §2.2、§2.3、§3.7 红线表）。
报告 §2.3 对"板子短路了，我要索赔"标注 **"✅ 机制就绪"**。

**实测：不成立。** 现在的结果是——

```
板子短路了，我要索赔            scene=technical_support  route=knowledge_qa   action=answer_from_knowledge
this is unacceptable, I want…  scene=complaint          route=knowledge_qa   action=answer_from_knowledge
```

即**投诉被知识库回答**。这与"必须转人工"直接冲突。

**红线守卫挡不住它，别误以为挡住了**（`orchestrator.py:1122-1142`）：

- 它是 **flag 门控、默认关闭**的（`flag_redline_guard`）；
- 它扫的是**生成的草稿文本**，不是请求——即"已经决定回答之后"才拦；
- 它拦的是"草稿里出现了价格/交期/归责/赔付的承诺"
  （`REDLINE_COMMERCIAL_COMMITMENT`），不是"这是一次投诉"。

所以默认行为就是：**投诉被知识库正常回答**，AI 还可能对赔付问题给出政策性解释。
计划要的是**根本不去回答**。

**建议做法**：在编排器里，`Scene.COMPLAINT` + 意图为知识问答时**弃权转人工**
（理由码自定，例如 `COMPLAINT_REQUIRES_HUMAN`），并保留"AI 只做登记与摘要"。
**先读计划 §2.2 / §3.7 红线表再动手**，并在 `docs/agent.md` 记录该路由。
注意 `我要投诉` 当前落 `business_write`（会走写路径），要一并想清楚哪种更对。

### 🟡 3.2 大客户分层「直转专属人工」（被缺失的关联阻塞）

SLA 那半已可用（`sla_policy_for_tier`，走 `Case.enterprise_account_id`）。
**路由那半缺前提：平台无从得知一个会话属于哪个 `EnterpriseAccount`。**
`ExternalResourceRef` 只映射 chatwoot account/conversation → tenant，没有 contact → account。

**关键实测**：`inbox_id` **在** webhook 最小化载荷里
（`support_bridge/minimize.py` 提取 `inbox_id`），所以"按 inbox 建关联"是可行的，
而且**在任何工单存在之前**就能知道客户是谁。

⚠️ **不要只建关联不建消费者**——那正是本项目反复出现的"有读无写"缺陷。
要么连着路由一起做，要么不做。

### 🟡 3.3 NextPCB 英文语料按知识空间隔离

`KnowledgeSpace` **没有 language 字段**，检索也不按语言过滤。
需要 schema 变更 + 检索侧过滤 + 一条"中英不串"的泄漏测试（跨租户测试是模板）。
**还需要一个决策**：问句的语言怎么判定。

### ⬜ 3.4 BOM 配单进度查询（被外部依赖阻塞）

`business_api` 适配器需要新增 `bom_read` 能力与 ERP 端点
（`integrations/business_read.py` 的 `READ_TOOL_RESOURCES` / `READ_TOOL_SCHEMAS`）。
**只建平台侧会得到一个永远 `TOOL_UNAVAILABLE` 的工具**——本项目最典型的缺陷形态。

### ⬜ 3.5 阶段 4 · 运营飞轮

工具已就位（缺口队列、质量看板、prompt 发布/回滚三页 + 各自服务），
**缺的是每周真的去跑**——那是运营节奏，不是代码。

---

## 4. 这个仓库的工作纪律（血泪，请照做）

1. **标志性缺陷是"有读无写"**：已发现十几处"被声明、被测试、生产路径打不到"的东西。
   任何字段/枚举/函数，**先 grep 写入者和调用者**；直接调用它的测试会掩盖"生产无调用者"。
   （本轮我又自己制造了一个 `CaseCategory.QUALITY_COMPLAINT`，已删。**加枚举值前先 grep。**）
2. **别信记忆，也别信旧文档，去测。** 本轮我两次被过时信息误导：
   以为"阶段 3 未开始"（其实 `case.create` 已存在并带 ADR）；
   按交叉复核清单挑活（其中一项早已被别人关掉）。
3. **改启发式前先对全量数据集测量，并双向加守卫**；语料里没有的句式，"没移动"只是弱证据。
4. **无法观察到失败的守卫不构成证据** → 每条守卫都做**变异验证**。
5. **过宽的守卫比没有守卫更糟**（我第一版 tenant-id 守卫扫出 14 处误报，说明前提就错了）。
6. **"这样更方便"与"文档/策略明确要求"冲突时，先假设文档是对的**，并去读它给的理由。
   （我因"那样智能体就够不到了"把计划要求的 `HUMAN_APPROVAL` 改成 `confirmed_write`，
   整套逻辑推翻重做。**可达性是便利论证，不是安全论证。**）
7. **改工具风险级必须配数据迁移**：`ensure_tool_definitions` 只增不改，而网关读行里的 `risk`。
   加迁移要同步 bump `EXPECTED_MIGRATIONS`。
8. **测试夹具删父表前先删子表**，否则毒害下一次运行。
   判据：**单跑也失败 = 数据库脏**；单跑通过 = 需要进一步归因。
9. **撤设计时把连带参数全删掉**，不留"以后可能用得上"的死代码。
10. **域拒绝必须返回 `error_response()`（`JSONResponse`）**；返回 dict 会被渲染成 200。
11. **有意不做的边界要写下来**（"记录，不夹带"），不要默默扩大改动范围。

---

## 5. 环境陷阱（每条都真实浪费过时间）

- **端口**：旧进程可能绑 `127.0.0.1:PORT` 而你的新实例绑 `0.0.0.0:PORT` 并打印
  "Application startup complete"——**loopback 流量打到旧代码上**，
  新端点回 405/404，看起来像路由没注册。`taskkill` **不一定杀得掉**。
  **判断依据永远是 `netstat -ano` 的绑定地址 + PID**；冲突就换端口。
- **`docker` CLI 可能整个挂死**（`docker ps` / `docker version` 都 SIGTERM 无输出），
  而**容器还在跑**（`netstat` 显示 5435/5434/6380 在监听）。
  绕法：用 `psycopg` 直连查库，不碰 docker CLI。
- **长跑会耗尽 Windows 套接字**：`OSError: [WinError 10055] 由于系统缓冲区空间不足或队列已满`
  （约 20 小时后），表现为 `/healthz` 502 或进程无声退出，**不是代码 bug**，重启即可。
- **长会话结束前收掉自己起的服务**：我留下的 API+Vite 跑了 **29 小时**，
  导致 shell `fork: Cannot allocate memory`。杀掉即恢复。
- **`curl` 一律 `--noproxy '*'`**，且用 `-o` 写文件（接管道会 exit 23、body 丢空）。
- **全量测试必须是最后一次 pytest**：门禁插件**无条件写**证据文件，
  定向运行会用部分证据覆盖它 → `release_check` exit 2。
  跑全量前先 `mv tests/artifacts/release_gate_evidence.json /tmp/`
  （否则 `pytest_configure` 的 `unlink()` 触发沙箱批量删除守卫，抛 `SystemExit`，
  而插件只捕 `OSError`，会话在收集前就死）。**不要改那个插件，它写得是对的。**
- pytest summary 走 **stderr**；用 `--junitxml` 拿权威计数（沙箱守卫会截断 summary）。
- **API 必须 `python -m platform_core.main`，绝不用裸 uvicorn**
  （ProactorEventLoop → psycopg async 拒绝 → 每个 DB 请求死在 connect，
  中间件把它渲染成光秃秃的 `401 AUTH_UNRESOLVED`）。
- **alembic**：在 `apps/api/migrations` 下 `-c alembic.ini`，
  并设 `APP_ALLOW_BOOTSTRAP_TOKENS=true`（本地）。
- **浏览器验收**：`playwright-core` 装在
  `~/.workbuddy-ai/binaries/node/workspace/node_modules`，而 **ESM 不认 `NODE_PATH`**
  → 把 `.mjs` 拷到该 workspace 的 `acceptance/` 下再跑。长脚本后台跑 + 重定向。
  Chrome 的 `innerText` 返回**渲染后**文本（`.section-title` 是 uppercase，断言忽略大小写）。
  断言要**限定作用域**（页面级 banner 可能是上一步的陈旧失败）。
- `PYTHONPATH` 必须**绝对路径且用 `;` 连接**（Git Bash 用 `cygpath -w`）。

---

## 6. 验证命令（照抄）

```bash
cd "D:/360Downloads/360驱动大师目录/b2b-ai-support-plan/b2b-ai-support-plan"
R="$(cygpath -w "$(pwd)")"
export PYTHONPATH="$R/apps/api/src;$R/packages/contracts/src;$R/packages/policy/src;$R/packages/observability/src;$R/apps/worker/src"

./.venv/Scripts/python.exe -m ruff check apps packages scripts tests pytest_plugins_release
./.venv/Scripts/python.exe -m ruff format --check apps packages scripts tests pytest_plugins_release
./.venv/Scripts/python.exe -m mypy

# 全量必须是最后一次 pytest（先移走证据文件）
mv tests/artifacts/release_gate_evidence.json /tmp/evidence.bak 2>/dev/null
./.venv/Scripts/python.exe -m pytest --junitxml=tests/artifacts/junit-final.xml
./.venv/Scripts/python.exe -m platform_core.evaluation.release_check --evidence-only
```

---

## 7. 建议的第一步

**做 3.1（投诉转人工）**：它是计划里明确要求的、当前**实测不成立**的、且**没有任何阻塞**的一项，
而且它同时是 3.2（大客户直转人工）的触发条件——两者都键在"这是不是一次投诉"。

动手顺序建议：**先读计划 §2.2 / §2.3 / §3.7 红线表** →
写清楚"为什么投诉必须转人工、转给谁、AI 保留做什么" → 再改代码 →
加守卫并做变异验证 → 更新 `docs/agent.md` 与报告附录。

**另一件小事**：把 18 个未提交文件按主题拆成提交（或先问用户怎么拆）。
现在 HEAD 领先 origin 18 个提交且未 push，**先确认远端可用再 push**。
