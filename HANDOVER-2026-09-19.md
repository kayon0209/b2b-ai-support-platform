# 交接文档 — B2B AI 客服平台（2026-09-19 夜）

给接手会话：这份文档是自包含的。你没看过之前的对话，请先读完再动手。
仓库：`D:/360Downloads/360驱动大师目录/b2b-ai-support-plan/b2b-ai-support-plan`

---

## 0. 先读这两条，能省你几小时

**① 这个平台不是聊天产品，是"AI 控制平面"。**
`docs/architecture.md` 第一句写明：与 Chatwoot 集成为外部边界。客户 → Chatwoot → 签名 webhook → 本平台；回程是 REST 回发 Chatwoot。
本仓库原本只有企业侧管理后台，**没有客户侧界面**——这就是之前"这个项目怎么这么奇怪、还要跑去 Chatwoot 提问"的根因。

**② 仓库没有 remote，无法 push。** 用户 GitHub 上还没建仓库。
当前保底是 bundle 备份（含全部 9 个提交）：
`D:/360Downloads/360驱动大师目录/b2b-ai-support-plan/b2b-backup-2026-09-19.bundle`
恢复：`git clone <bundle>`。建好远端后请 `git remote add origin <url> && git push -u origin master`。

---

## 1. 今天已完成（9 个提交，都未 push）

```
1059ad8  Stop swallowing the local turn read error in the worker
19239d2  Chat: collapse the duplicated customer turn
9ce3e18  Align customer conversation ref with the worker's derivation   ← 闭环达成
82c3bc8  Migration 0037: resolve_turn_text
e9d8a3e  Revert the reader wrapper（修我自己引入的回归）
1bd2692  Worker: local-first reader（部分）
63407de  Customer message write endpoint
8e4928b  Customer chat surface + read-only timeline/citations
ea71f30  Baseline: admin-web 缺陷修复 + worker Windows 启动修复
```

更早还修了 **38 项验收缺陷（P0–P3）** 与 **4 个真 bug**：
领域错误返回裸 dict 致 HTTP 200 假成功、worker 在 Windows 起不来（ProactorEventLoop）、
`/chat` 误显示企业侧边栏、消息写入幂等不生效。

### 关键文件（都是新增或我主导改的）
- `apps/admin-web/src/pages/CustomerChat.tsx` — 客户聊天页
- `apps/api/src/platform_core/agent_runtime/customer_router.py` — 3 个客户侧接口
- `apps/api/migrations/versions/0037_turn_text_resolver.py` — SECURITY DEFINER resolver
- `apps/worker/src/worker/inbox_consumer.py` — 本地优先读取

### 三个接口
```
POST /v1/customer/conversations/{ref}/messages   写客户消息（脱敏 + 幂等）
GET  /v1/customer/conversations/{ref}/timeline   读会话（RLS 隔离）
GET  /v1/customer/agent-runs/{id}/citations      读引用
```

### 客户聊天页形态（对齐真实产品）
气泡 56×56 → 桌面 **380px tray** → <480px 全屏 → 可放大居中；`dvh` 高度；
ARIA dialog + Esc + 焦点管理；空态/输入中/失败重试/离线/转人工全齐；中英双语。
入口：`http://localhost:5174/chat`（**已移出 Layout**，否则客户会看到企业侧边栏）。

---

## 2. 当前状态：什么能用，什么不能

| | 状态 |
|---|---|
| 客户聊天页界面 | ✅ 完整可用 |
| 客户消息入库 + 脱敏 + 幂等 | ✅ 实测通过（`a@b.com` → `[EMAIL]`） |
| 历史对话展示 | ✅ 真实数据 |
| **AI 回复** | ⚠️ **曾验证成功，当前不出**（见下） |
| 测试 | 1478 passed / 8 skipped / 2 failed（**均为已知 flake**） |
| 门 | ruff + format 通过 · tsc 0 · mypy 我的文件 0 错 |

**"曾验证成功"**：提交 `9ce3e18` 时端到端实测通过——
```
[customer] 四层板的交期是多久？
[agent]    Could you give me a little more detail? I want to make sure I answer the right question.
```
（请求澄清是正确行为：知识库没有四层板内容，宁可问清也不编。）

**现在不出答案的现象**：`inbox_events` 全部 `completed`，且 worker 日志有
`event_processed`。**注意：不要把"agent_runs 里看到 queued"当成结论——
那多半是别的会话的 run。** 用 run_id 精确查，我自己的 run 实际是 **`failed`**。

---

## 3. 未完成的两件事（按优先级）

### 任务 A：定位"编排执行失败"（优先级最高）

> **更正（重要）**：上一轮曾记录为"event completed 但 run 卡在 queued"，
> **这个诊断是错的**。收工后读 worker 日志才发现：我自己的 run 实际是 **`failed`**，
> 之前看到的 `queued` 是另一个会话别的会话的 run。**别按"卡住"去查，是"失败"。**

**日志里的硬证据**（worker 日志，`event_processed`）：
```
status="failed"  route="knowledge_qa"  latency_ms=9235
status="failed"  route="knowledge_qa"  latency_ms=50000   ← 50 秒
status="failed"  route="knowledge_qa"  latency_ms=6625
```
按 run_id 精确查 `agent_runs`，三个都是 `('failed', 'knowledge_qa', None, <latency>, None)`
（abstain_reason 为空 → **不是弃权，是执行失败**；output_hash 为空 → 没产出）。

**最可能方向：LLM / embedding 调用超时或抛异常**（50s 时延很典型）。
下一步：复现一次并抓具体异常栈（编排里 failed 分支应该有日志，或者临时加 error 级日志）。

**已排除的（别重复查）**：
- ❌ 不是 `resolve_turn_text` 的问题 —— 以 `platform_app` 实测可用，
  返回 `'四层板的交期是多久？'`，函数对 app role 可见。
- ❌ 不是另一个会话的回归 —— mypy 4 错全在它的文件
  （`tool_gateway/case_create.py` 引用不存在的 `CasePriority`、`registry.py` 参数不匹配）；
  pytest 1478 passed，2 个失败都是已知 flake。
- ❌ 不是知识库为空 —— admin-demo 有 2 个 chunk 且都有 embedding + search_vector。

**已埋好的探针**：`inbox_consumer._local_turn_text` 原来 bare `except: return None`
把错误吞了，已改为先 `logger.warning("local_turn_read_failed", ...)` 再回退（提交 1059ad8）。
**下一步就是重启 worker、发一个问题、看这条日志。**

**最可能的位置**：`inbox_consumer` 的 `resolve_question` 返回 None 后提前 return，
导致 event 被 ack 但编排没跑。两个候选分支：
```python
conversation_ref_id = _conversation_ref(event)
if conversation_ref_id is None: return None
question = await resolve_question(event, deps)
if question is None: return None          # ← 最可能在这里
```

**注意**：`_conversation_ref` 会 **uuid5 派生**，不原样使用外部 id：
```python
uuid5(tenant_id, f"chatwoot:conversation:{external}")
```
`customer_router` 已同步派生（提交 9ce3e18）。**任何新入口如果自己造 UUID 会踩同一个坑**。

### 任务 B：检索阈值（等 A 完成后再做）

```
hybrid_search("What are your support hours?") → 2 hits
  score = 0.0328 / 0.0323        ← RRF 融合值，远低于阈值
```
数据没问题、召回正常，**卡在分数判定**。判定点：
- `qa_path.py`：`ABSTAIN_LOW_RELEVANCE = "EVIDENCE_BELOW_THRESHOLD"`
- `orchestrator.py`：`retrieval_score_floor_ratio` + `flag_score_floor`

⚠️ **这属于质量调优，而另一个会话正在做 QA/评测，容易撞车。动手前先跟用户确认。**

---

## 4. 另一个并行会话（重要）

它 17:43–17:58 还活跃（改 `orchestrator.py`、`policy/engine.py`、建迁移 `0038`），
18:40 后未观测到改动。目前有 **58 个未提交文件**。

**迁移衔接是好的，别破坏**：
```
0036_tool_citations → 0037_turn_text_resolver（我的）→ 0038_eq_confirm_human_approval（它的）
```
它的 0038 的 `down_revision` 就是我的 0037；`EXPECTED_MIGRATIONS = 38`；DB head = 0038。

**它的工作区当前编译不过**（mypy 4 错，都在 `tool_gateway/*`）。别去修它的文件，
除非用户明确要求——你不知道哪些是半成品。

---

## 5. 环境与坑（血泪清单）

```bash
# PYTHONPATH 必须绝对且 ; 分隔，否则 No module named 'platform_core'
# Windows 上任何 DB 脚本都要 selector loop：
asyncio.run(coro, loop_factory=asyncio.SelectorEventLoop)
```
- Python 用 `.venv/Scripts/python.exe`（3.12）。3.13 没装 pytest。
- 端口：ai-pg **5435** · Chatwoot 3000 · API **8010** · Vite **5174**
- DB 角色：`platform`（superuser，绕过 RLS）/ `platform_app`（受 RLS 约束）
- Chatwoot：账号见 compose 里 seed 的管理员，口令首启时设定、**不记录在此**
  （仓库可读 = 口令可读）。account id **3**，inbox **2**
  （webhook 指向 `host.docker.internal:8010`，**写 localhost 会打到容器自己**）
- 租户 token：**不入库**。跑 `python scripts/seed_admin_demo.py` 现场打印；
  每台机器的 user id 不同，token 因此每台不同（`pt_<slug>_<user-id>`）。
  历史上写死在这里的那串 `pt_admin-demo_<uuid>` 已随种子改为随机 id 而失效 ——
  它的 user id 原先是从仓库常量推导的，等于公开了一个可计算的 `tenant_owner` 凭证。
- alembic.ini 在 `apps/api/migrations/`；非 ASCII 路径下要用
  `cfg.set_main_option("script_location", ...)` 且**不要 chdir**（否则找不到 .env）
- **跑测试前先停 worker**，否则 `claimed=0` 类 flake 会让不同文件随机失败
- **长时间运行会耗尽 Windows 套接字**，API 崩于
  `OSError: [WinError 10055] 由于系统缓冲区空间不足或队列已满`（跑了约 20 小时后）。
  表现为 `/healthz` 502 或进程无声退出，**不是代码 bug**。
  遇到"API 又挂了"先确认是不是这个，重启即可；调试期建议定期重启服务。

---

## 6. 建议的第一步

1. 建 GitHub 仓库 → `git remote add` → push（**先做，当前只有 bundle 保底**）
2. 重启 worker（加载 1059ad8 的日志）→ 发一个问题 → 看 `local_turn_read_failed`
3. 根据日志定位任务 A；A 通了再考虑任务 B

```bash
# 起服务（参照）
cd "<repo>"
PYTHONPATH="<repo>/apps/worker/src;<repo>/apps/api/src;<repo>/packages/contracts/src;<repo>/packages/policy/src;<repo>/packages/observability/src" \
  APP_CHATWOOT_API_TOKEN=UTojCrMg3PKDgvvTcjdyWtcQ \
  ./.venv/Scripts/python.exe -m worker.runner
```

---

## 7. 请保持的工作方式（用户认可）

- **先查再动**：动手前 grep 计划文档和 alembic head，避免重复劳动与迁移编号冲突。
  今天靠这条发现另一会话已做完 `conversation_turns` 持久化，省掉一大块工作。
- **不替项目辩护**：发现架构缺口就直说。今天纠正了自己三个错误判断
  （"知识库是空的"、"另一会话停了"、"有问题就改 reader 身份"）。
- **不吞异常**：本仓库反复出现"静默成功"缺陷。catch 了就要记日志。
- **改依赖注入的身份前先看 `is` / `isinstance` 断言**：
  今天换 `deps.reader` 破坏了 `sender is reader` 和 `isinstance(sender, ChatwootClient)`，
  两条都是真实契约，已回退（e9d8a3e）。
- **报告要诚实**：不确定的标"疑似/未验证"，别把猜测说成结论。
