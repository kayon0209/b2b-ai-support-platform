# 用户旅程走查报告 — 2026-09-21

范围：按真实用户旅程走查（注册/登录 → 首次使用 → 主流程 → 异常边界 → 网络失败 → 并发/重复提交 → 返回刷新 → 退出），
重点排查功能 bug、安全风险、性能、卡顿与资源泄漏、UI/交互反馈。

**方法**：起真实 API（`python -m platform_core.main`，:8000）实测 + 读代码定位。
下文每条都标注了**证据来源**；凡是没能实测的，我明确写"未实测"，不含糊。

**未覆盖**：未做压测/负载测试；未走查 Chatwoot 容器内的真实客户侧（本地无客户前端，
客户面由 `/chat` 承担，见 F-1）；未验证 MinIO/Keycloak 交互。

---

## 一、先说结论

**做得扎实的部分**（实测通过，不是读代码推测）：

| 项 | 证据 |
|---|---|
| 幂等 | 同一 `Idempotency-Key` 二次提交 → 返回**同一** `turn_id` + `duplicate:true`；缺 key → 400 且提示明确 |
| 内容级重放保护 | 客户消息按（会话+文本哈希）去重：**两个不同 key、同一文案只写 1 条**（实测） |
| 租户隔离 | 跨租户读为空（RLS），未发现越权 |
| 权限 | viewer 写 → 403；记录/审核权限按影响面分离 |
| 参数校验 | `limit=0/-1/99999/abc` 全部 422，且**错误体带可读信息 + trace_id** |
| 路由/方法 | 未知路由 404、错误方法 405 |
| 前端 API 客户端 | 20s 超时、DNS/拒连→可读提示、401 统一弹窗、trace_id 自动附加、`useAsync` 丢弃过期响应 |
| 空态 | 11 个页面全部有空态处理 |
| 限流 | 默认开启，600 次/60s；12 次连打不触发（符合预期） |

**必须修的问题**：F-1（客户面进不去）、F-2（无限"正在输入"）、F-3（双击重复建草稿）、F-4（无法退出）。

---

## 二、功能性问题

### F-1【高】客户对话窗对真客户不可用，且失败被静默吞掉

**触发**：客户直接打开 `/chat`（无运营令牌）。

**复现**（实测）：
```
curl -s -w "\n%{http_code}\n" http://127.0.0.1:8000/v1/customer/conversations/test-ref/timeline
→ 401 {"error":{"code":"AUTH_UNRESOLVED",...}}
```
带无效令牌同样 401。`/v1/customer/*` 三个端点都要求 `CASE_READ`（运营权限）。

**后果与线索**：
1. 真客户无法收发消息——这是"客户面"却没有客户可用的身份体系。
2. `CustomerChat` 的 `loadTimeline` 用 `catch {}` **静默吞掉**失败（注释写"空会话不算错误"），
   于是 401 表现为**永远空白的聊天窗**；发送失败只显示"未送达"，不区分 401/超时/5xx。
3. `/chat` 在 `Layout` 之外，而 `setUnauthorizedHandler` 只在 `Layout` 里注册
   → `/chat` 上 401 **不会**弹出令牌对话框，用户没有任何自救入口。
4. 若构建时设了 `VITE_API_TOKEN`，令牌会被打进 JS bundle 发给每个访客（含客户面）——见 S-2。

**建议**：
- 明确 `/chat` 的定位。若它是**本地演示/内部验证面**：改名并加显著标识，失败时给出明确文案
  （"此界面需要运营令牌"），并把 401 从静默改为可见提示。
- 若要成为真实客户面：客户身份必须独立于运营令牌（会话级 token / 签名链接 / 交给 Chatwoot），
  并且**绝不**与运营 bundle 共用。
- `loadTimeline` 的 `catch {}` 至少要区分 401 与"空会话"。

---

### F-2【中】平台不回复时，"正在输入…"永不停止，并持续 2 秒轮询

**触发**：消息已入库并排队，但**始终不产生 agent 轮次**。三种现实路径都会命中：
影子模式（`agent.shadow_mode_enabled`，**刻意不发送**）、worker 未运行、run 失败。

**复现**：开影子模式 → 客户发消息 → 消息"已发送" → 界面永远显示"客服正在输入…"。

**线索**：`CustomerChat.tsx:163-168` 的停止条件是
```ts
if (typing && messages.some((m) => m.role === "agent")) { setTyping(false); ... }
```
`waitingRef` 只在"收到 agent 消息"或"发送请求抛错"时复位。没有回复 → 永不复位
→ `useEffect`（依赖 `typing`）里的 `setInterval(…, 2000)` **无限轮询**。

**影响**：客户看到永久假状态（比明确失败更糟）；每个打开的会话每 2 秒打一次后端，
是真实的资源浪费与无原因卡顿。

**建议**：加等待上限（如 30–45s，与 `llm_timeout_seconds` 对齐），超时后
把状态改为"暂时无人应答/已转人工"并提供重试；轮询改为退避（2s→4s→8s，封顶）。

---

### F-3【中】双击会创建两份草稿（缺前端防重 + 后端无内容去重）

**触发**：`/gaps` 页面双击"创建草稿"（或 `/prompts` 的 6 个写按钮）。

**线索**（读代码）：
- `GapQueue.tsx:69`：`apiPost(path, body, newIdempotencyKey())` —— **每次点击生成新键**。
  幂等键防的是"网络重试"，**不防双击**；防双击只能靠 `busy` 禁用。
- 全站 12 处 `disabled={…busy}`，但 **GapQueue(8 个按钮) / PromptRelease(6 个) 一处都没有**。
- `gap_service.create_draft` **无重复保护**：只挡 `gap.status == RESOLVED`，
  而首次创建后状态是 `DRAFTED`，第二次调用照样通过 → 两份草稿。

**实测旁证**：客户消息端点对同一文案的两个不同 key 只写 1 条（内容哈希去重），
说明**消息路径有兜底、草稿路径没有**——这不是同一套保护。

**建议**：① 用 `useAction().busy` 禁用触发按钮（所有写按钮统一）；
② 幂等键按**操作身份**派生（如 `draft:${gapId}`）而非每次点击新生成；
③ 后端给 `create_draft` 加同 gap 同 title 的重复保护（与消息路径对齐）。

---

### F-4【中】没有任何"退出登录"入口

**触发**：共享工作站上换人使用。

**线索**：全站 grep `logout|signOut|退出|登出` **零命中**。`TokenDialog.save()` 在令牌为空时
直接报错 `token.errorEmpty`，**没有清除令牌的按钮**。令牌只存在 `localStorage`，
且**没有过期/失效处理**（收到 401 只弹窗要求粘贴新令牌）。

**影响**：无法主动结束会话；下一个使用者继承上一个操作员的权限。

**建议**：令牌弹窗加"退出"（`setToken("")` + reload）；令牌加有效期与到期提示；
上 OIDC 后用真实登出。

---

## 三、安全

### S-1【中】未签名 bootstrap 令牌可冒充（防护到位，但留了部署陷阱）

**事实**：令牌格式 `pt_<租户slug>_<user-id>`，**未签名**；解析只做"slug→租户 + user_id→成员"查库
（`identity/middleware.py:141-197`）。知道 slug 和成员 UUID 即可冒充该成员及其角色，无需任何密钥。

**已做的防护（我实测/核对过，扎实）**：
- `allow_bootstrap_tokens` 默认 **False**；
- `_assert_auth_is_configured` 在 staging/production 下开启即**拒绝启动**，并明说是冒充路径；
- 未配置任何鉴权策略时**拒绝启动**（fail closed），不会静默放行。

**残留风险**：`environment` **默认 `"local"`**。若部署时忘记设 `APP_ENVIRONMENT`，
而 `.env` 沿用了本地的 `APP_ALLOW_BOOTSTRAP_TOKENS=true`，守卫会**通过**（它认为这是 local）。

**建议**：`environment` 不设默认值（强制显式声明）；或 bootstrap 令牌额外要求
`APP_ENVIRONMENT` 被**显式**设置过。

### S-2【中】令牌存 localStorage + `VITE_API_TOKEN` 编译期兜底

`lib/api.ts:18`：`getToken()` 回退到 `import.meta.env.VITE_API_TOKEN`。
**构建期注入的令牌会写进 JS bundle**，任何访客（含 `/chat` 客户）都能从 bundle 里读出。
配合 S-1/F-1，这是把运营凭据发给客户面。

**建议**：删除该编译期兜底，或仅限 `import.meta.env.DEV`；长期改为 httpOnly Cookie / OIDC。

### S-3【低】租户 slug 含下划线会导致令牌解析错误

`token.split("_", 2)`（middleware.py:154）在 slug 含 `_` 时会切错。
现有 slug 是 `admin-demo` 这类（无下划线）所以没暴露。**建议**：在租户 slug 校验里禁用 `_`，
或改用 `rsplit("_", 1)`。**未实测**（本地库无含下划线 slug 的租户）。

---

## 四、交互与体验

### U-1【中】全站没有 URL 状态：刷新丢现场、返回键无效、工单无法分享

`grep useSearchParams|URLSearchParams|location.search` → **零命中**。筛选、页码、
工作台选中的工单全是组件状态（`Workbench.tsx:52` `useState<string|null>(null)`，
且自动选中第一条）。

**后果**：
- 坐席看到一半按 F5 → 回到第一条工单、筛选清空（**正在处理的工作丢失现场**）；
- 浏览器"返回"不是"退回上一个筛选/工单"，而是离开页面；
- 无法把工单链接发给同事（支持团队最常用的协作方式）。

**建议**：把 `status/page/caseId` 写进 `useSearchParams`（工作台至少把 caseId 放进路径 `/workbench/:caseId`）。

### U-2【低】错误信封不统一：404/405 退化成"HTTP 404"且无 trace_id

实测：422 是标准信封（`VALIDATION_FAILED` + 可读信息 + trace_id），
但 **404/405 返回 FastAPI 默认的 `{"detail":"Not Found"}`**。
前端 `toApiError` 只认 `error.message` / `error.reason` → 显示"HTTP 404"，
且**没有 trace_id**（`trace_id` 取不到 → 不附加）。

**建议**：给 `HTTPException`/404/405 加统一异常处理器，输出同款信封。

### U-3【低】"未送达"不区分原因

发送失败只有一句"未送达/Not delivered"，401（无权限）、超时、5xx 都是同一句。
**建议**：把 `ApiError.message` 透出（客户端已带 trace_id），至少区分"权限/网络/服务端"。

---

## 五、性能与资源

- **未发现后端瓶颈**：`/v1/quality/metrics` 实测 **189ms / 455B**（空数据，仅供参考）；
  `latency_p50/p95` 为 `null` 因窗口内无 run。
- **F-2 是唯一确认的资源浪费**：无回复时每 2 秒轮询不停止。
- **未做**：压测、长跑内存观察、连接数耗尽验证。**结论仅限本次走查范围。**

---

## 六、建议的修复顺序

1. **F-1 + S-2**（客户面身份与令牌暴露）——影响面最大，且互相放大。
2. **F-2**（无限假状态 + 轮询）——客户可感知，且是持续负载。
3. **F-3**（双击重复建草稿）——数据污染，改动小。
4. **F-4 / U-1**——共享设备与坐席日常工作流。
5. **S-1 部署陷阱 / U-2 / U-3**——加固与一致性。

---

## 七、复现环境

- API：`python -m platform_core.main`（:8000，本地），令牌 `pt_admin-demo_<uuid>`
- 数据库：`postgresql://platform:platform@localhost:5435/platform`
- 实测命令与输出见正文各条；探针写入的 3 条会话轮次已清理。

**本次走查未改动任何产品代码**（按要求未提交）。

---

# 修复记录（同一轮，未提交）

全量 **1734 / 0 失败 / 0 错误 / 0 跳过**（新增 5 条测试），`release_check` exit 0，
ruff check + format + mypy、`tsc --noEmit`、`vite build` 全绿。

| 编号 | 状态 | 改动 |
|---|---|---|
| U-2 | **已修** | `main.py` 增加 `StarletteHTTPException` 处理器：404/405 也走统一信封，带 `trace_id`；状态码映射为 `NOT_FOUND`/`METHOD_NOT_ALLOWED` 等 |
| S-3 | **已修** | 令牌解析改为 `partition("_")` + `rpartition("_")`：slug 含下划线不再解析错（原来 `split("_", 2)` 会把 id 切碎） |
| S-1 | **已修** | `allow_bootstrap_tokens` 现在要求 `APP_ENVIRONMENT` **被显式声明**（用 `model_fields_set` 判断"声明"与"默认"）。3 条测试覆盖：未声明→拒绝、显式 local→通过、production→仍拒绝 |
| F-3 | **已修（两侧）** | 后端：`create_draft` 加（gap+title+pending）重复保护，第二次提交返回同一条；前端：GapQueue 6 个写按钮、PromptRelease 2 个写按钮加 `action.busy` 禁用。2 条测试：同标题→一条、不同标题→两条 |
| S-2 | **已修** | `getToken()` 仅在 **dev 构建**下才认 `VITE_API_TOKEN`；生产 bundle 一律以未登录启动（编译期令牌不再随 bundle 发给访客） |
| F-4 | **已修** | 令牌弹窗增加「退出登录」：清空令牌 + reload；仅在已持有令牌时显示 |
| F-1 | **已修（可见性部分）** | `loadTimeline` 不再 `catch {}` 静默吞错；401 明确提示"此界面需要运营令牌"；错误横幅不再只在演示模式下显示 |
| F-2 | **已修** | 等待上限 45s（与 `llm_timeout_seconds` 对齐）+ 轮询退避（2s→4s→8s→10s 封顶）；超时后停止轮询、清除"正在输入"、给出「暂时无人应答」状态 |
| U-3 | **已修** | 新增 `describeFailure()`：401 与网络/服务端错误分开表述，并透出服务端消息（含 trace_id） |
| U-1 | **已修** | 工作台 `/workbench/:caseId`；Cases 的 `?case=`/`?offset=`（offset=0 不写进 URL，避免链接里带噪音）；GapQueue 的 `?tab=drafts`。刷新保留现场、返回键可用、工单可分享 |

## 仍未修（明确记录，避免误以为已清）

1. **F-1 的根因未动**：`/chat` 仍然要求运营令牌。让真客户可用需要**身份方案决策**
   （会话级 token / 签名链接 / 交回 Chatwoot），这是产品决策而非 bug 修复，我不擅自把端点改为公开
   ——那会打开"任何人可灌消息并触发 LLM 跑"的口子。
2. ~~**U-1 剩余页面**~~ —— 已补完（Cases、GapQueue 见上表）。
3. **F-4 的令牌有效期**：仍无过期/续期机制，等上 OIDC 一并解决。
4. **未验证**：本轮修复只跑了测试与构建，**未在浏览器里实际操作确认**
   （F-2 的超时、F-4 的退出、U-1 的刷新/返回都属交互行为，建议手动过一遍）。
5. **未做**：压测、长跑内存观察。

**本轮改动未提交**（按你先前"先暂不提交"的要求）。

---

# 浏览器实测（第二轮）— 并发现一个新 bug

方法：用 `playwright-core`（受管 workspace 内已有）+ 本机已装 Chromium 驱动真实浏览器，
起 API(:8000) 与 Vite(:5173)，逐条验证。**结果 11/11 通过。**

## 新发现并已修：F-5【高】坐席工作台队列恒为空

**现象**：库里 21 条工单，工作台显示"工单队列 暂无工单"。

**根因**：`Workbench.tsx` 读 `apiGet<{cases: CaseSummary[]}>("/v1/cases")` 后取 `data?.cases`，
而 `/v1/cases` 返回的是 **`{items, total}`**（与全站其他列表路由一致）。字段名不存在 → `undefined` → 恒为空。

**为什么测试没抓到**：`apiGet<T>` 的泛型是**断言**不是校验，TypeScript 无法发现一个
从不在响应里的字段名；而我的集成测试只覆盖了 `/v1/cases/{id}/workbench` **端点**，
没有任何测试覆盖这个**页面**。全站扫描确认：其他所有列表页都用 `items`，工作台是唯一异类。

**修复**：改为 `{items, total}` / `data?.items`。浏览器实测：队列从 0 条变为 **23 条**。

> 这条是本轮最有价值的产出：**一个通过全部 1734 条测试、却在真实 UI 里完全不可用的核心界面。**

## 浏览器实测通过项

| 验证 | 结果 |
|---|---|
| F-1 无令牌打开 `/chat` | 显示中文提示「此界面需要运营访问令牌…」，不再是空白 |
| 工作台加载工单 | 23 条 |
| U-1 工作台 | URL 变 `/workbench/<id>`；**刷新后仍停在同一工单**；**返回键退回上一个工单**（仍在本页内） |
| U-1 漏点队列 | `?tab=drafts`；刷新后仍在草稿页 |
| U-1 工单列表 | `?case=<id>`；刷新后仍打开同一工单 |
| U-2 404 信封 | 浏览器内 `fetch` 返回 `{"error":{"code":"NOT_FOUND"},"trace_id":"..."}` |
| F-4 退出登录 | 点击后 `localStorage.b2b_token === null` |

## 顺带发现（未修，低优先级）

- **Vite dev server 只监听 IPv6 `[::1]:5173`**：`http://127.0.0.1:5173` 不通，只能用 `localhost`。
  本机开发容易误判为"前端没起来"。建议 `server.host: "127.0.0.1"` 或文档写明。
- **未鉴权时先鉴权后路由**：未带令牌访问不存在的路径返回 401 而非 404。
  这是**正确**的安全取舍（不向未鉴权调用者泄露有哪些路径），我原本的脚本反而据此误报了一次。

## F-2 的超时行为（第三轮，已实测）

场景取"平台永不回复"中最真实的一种：**worker 未运行**，run 排队后不产生 agent 轮次。

为了让"时间流逝"这件事可测，等待上限改为可配置（`VITE_CHAT_WAIT_TIMEOUT_MS`，**生产默认仍是 45s**）——
这是测时间分支的诚实做法：时长是配置，但**代码路径、UI 状态、轮询停止都是真的**。

实测结果（等待上限设为 6s）：

| 检查 | 结果 |
|---|---|
| 超时后给出提示（"暂时还没有人应答…"） | **通过** |
| 轮询停止 | **通过**（超时后 10 秒窗口内 **0 次**请求） |
| 全程 timeline 请求数 | 4 次（符合 2s→4s 退避后于 6s 超时停止） |

**未能有效验证**：过程中"正在输入…"指示器是否显示——该元素用的是 `aria-label` 而非文本，
我的脚本读 `innerText` 读不到。这是**脚本的选择器问题，不是产品问题**，但我不能声称验证过它。

## 仍未验证

- 移动端/窄屏布局、无障碍（键盘导航、屏幕阅读器）。
- 压测、长跑内存。

---

# 页面走查（第四轮）— 未访问过的页面 + 又发现两个问题

把从未在浏览器里打开过的页面逐个走了一遍（审批 / 成员 / 用量 / 品牌 / 提示词发布 / 特性开关）。

**六个页面的数据都能正常加载**（无 F-5 同类接线错误）：

| 页面 | 请求 | 渲染 |
|---|---|---|
| 审批 | `/v1/tool-proposals` ×4 全 200 | 计数 + 空态正确 |
| 成员 | `/v1/identity/members` 200 | 2 名成员 + 角色下拉 |
| 用量 | `/v1/tenant/billing` + `/usage` 200 | 41/100 运行数、账本 4 条 |
| 提示词发布 | `/v1/prompts` + `/active` 200 | 版本列表 + 状态 |
| 特性开关 | `/v1/flags?limit=200` 200 | 5 条开关 |
| 品牌 | `/v1/tenant/branding` 200 | 表单 + 预览 |

但品牌页暴露了两个问题。

## F-6【中】`display_name` 是四个品牌字段里唯一没有校验的

**发现**：品牌预览显示 `无 Logo <img src=x onerror=alert(2)>Acme & Co` —— 某个租户真的把 XSS 载荷存成了显示名。

**定性（我查证过，不夸大）**：
- 前端**没有任何** `dangerouslySetInnerHTML` / `innerHTML` / `document.write`，所以该值经 React 转义、**在后台不可执行**；
- 品牌值**没有推给 Chatwoot**、也不进邮件；
- 因此**当前不是可利用的 XSS**，而是**输入校验缺口 + 潜伏风险**：将来任何 HTML 消费端（邮件模板、PDF、Chatwoot 收件箱名）都会执行它。

**根因**：`_validate()` 对 `primary_color`（锚定十六进制，防 `javascript:`）、`logo_url`（仅 http/https）、`support_email`（邮箱正则）都有规则，**唯独 `display_name` 只有 `max_length=255`**。

**修复**：拒绝 `<>` 与控制字符，拒绝空白；**允许** `&`、引号、任意文字与语言（`Acme & Co`、`华秋电子` 均正常）。7 条参数化测试。

## F-7【中】品牌配置到不了唯一的客户面（设置等于无效）

**发现**：全站只有品牌页读 `/v1/tenant/branding`；`CustomerChat` 里品牌是**硬编码**的
`{ name: "Acme Electronics", color: "#2f6feb" }`。品牌页写着"对客户呈现的形象"，但客户面根本不读它。

**修复**：客户对话窗改为读取租户品牌，硬编码值降级为"未配置时的兜底"。

**浏览器实测**（该租户配置为名字 `<img src=x onerror=alert(2)>Acme & Co`、主色 `#1a393d`）：
- 客户面显示名 = 配置值 ✓（且**以文本渲染**，未执行 → 同时印证 F-6 的定性）
- 主色 = `#1a393d` ✓（不再是硬编码的 `#2f6feb`）

**遗留**：那条脏数据仍在库里（`admin-demo` 租户）。我没有擅自改租户数据——
新校验只拦后续写入。**建议你手工清一下**，否则客户面会一直显示这个载荷字符串。

## 本轮全量的一次红灯（如实记录）

第一次全量出现 1 条失败：`test_a_compensation_claim_is_handed_off_and_nothing_is_retrieved`。
按项目既有经验**先单跑** → 8 条全绿；**再复跑全量** → **1741 / 0 失败**。
判定为已知的全量偶发（夹具残留），非本次改动引入。

---

# 无障碍与窄屏（第五轮）— 关闭最后两个未验证项

方法：真实浏览器测量渲染后的 DOM（无第三方库，每条结论都能解释来源）。

## 通过项

| 检查 | 结果 |
|---|---|
| 窄屏 375×720，10 个路由 | **全部无横向溢出**（`scrollWidth == innerWidth`） |
| `html[lang]` | `zh-CN` |
| 按钮可访问名 | 全部有文本或 `aria-label` |
| Tab 首站 | **"跳到主要内容"跳转链接**（键盘用户可直接跳过侧栏） |
| 聊天启动器 | `aria-label="打开客服对话"` + `aria-expanded` 齐全 |
| 表单控件可访问名 | 修复后 6 个页面**全部命名** |

## F-8【低】提示词发布页的两个控件没有可访问名

**发现**：`/prompts` 的模板名输入框与草稿正文文本域只有 `placeholder`，没有 `label`/`aria-label`。

**为什么算问题**：`placeholder` **不是**可访问名——屏幕阅读器朗读不可靠，且用户一输入就消失。
这是控制台里唯一两个缺名的控件（其余页面全部命名）。

**修复**：补 `aria-label` + 中英文案键，复测通过。

**过程中自己踩的坑**：我用字符串替换加文案键时，把 `prompts.templatePlaceholder` **整个删掉了**
（中英两处的值恰好都是 `"template_name"`，两次替换都命中了它）。
**`tsc` 立刻报错拦住了**——因为这个字典是类型化的，`DictKey` 由字典推导，
删键 = 类型不存在 = 编译失败。这是这个项目一个很好的性质，值得知道。

---

# 并发与负载（第六轮）— 发现一个真实数据完整性 bug

方法：对**运行中的真实 API** 发并发请求（不是 `TestClient`——见下）。脚本已留存：
`scripts/concurrency_probe.py`（读环境变量，不硬编码令牌）。

## F-9【高】幂等去重在并发下失效，已产生重复问题

**实测（修复前）**：

| 场景 | 结果 |
|---|---|
| **同一**幂等键，10 个并发 | **写入 4 条**（应为 1） |
| **不同**键、同一文案，10 个并发 | **写入 2 条**（应为 1） |

**根因**：去重是 **SELECT-then-INSERT**，中间没有原子性。10 个并发请求**都查不到**，
于是都插入。顺序测试永远看不到这个（所以 1741 条全绿）。

**而且这不是理论问题**：库里**已经存在 10+ 组重复**（每组 2 条，非我探针所造），
说明这个 race 在真实使用中已经发生过——**客户双击发送或客户端重试，就会给坐席造出重复问题**，
而这正是这道守卫存在的理由。

**修复**：写入路径加**事务级咨询锁** `pg_advisory_xact_lock(hashtextextended(tenant:conv:hash))`，
让"查+插"对同一逻辑消息变成原子操作。

**为什么不用唯一索引**（我考虑过并放弃）：
- 唯一索引会**同时约束 Chatwoot 摄入路径**——那条路径记录的是本端点看不到的消息，
  且有自己的理由保留每一条；用本端点的去重语义去约束它，是越界。
- 而且库里已有重复数据，唯一索引会**直接创建失败**。
- 咨询锁只串行化"同一逻辑消息的并发写入者"，对其它路径零影响，也无需迁移。

**修复后实测**：同键 10 并发 → **1 条**；不同键同文案 10 并发 → **1 条** ✓

## 其余负载结果

| 项 | 结果 |
|---|---|
| `/v1/quality/metrics` @并发 1 | p50 **18ms**，无错误 |
| 同上 @并发 20 | p50 **184ms** / p95 **303ms**，**无错误** |
| 限流（配置 600 次/60s） | 700 次请求 → **116–190 次 429** ✓ **首次验证限流真的会触发**（此前只验证过它不触发） |

并发 20 时 p50 从 18ms 升到 ~200ms：单进程 uvicorn 串行处理，属预期；无错误、无超时。

## 为什么这是一个脚本而不是 pytest 用例

并发缺陷只在**真实并行**下出现，而 `TestClient` 通过单一 portal 驱动 ASGI 应用，
用它写的用例**可能"没修复也能通过"**——**一个不会失败的守卫比没有守卫更糟**（会给人假信心）。
所以这个检查面向运行中的进程，并写进了 `scripts/concurrency_probe.py`。

## 遗留（我不擅自处理）

- **库里已有的 10+ 组重复客户轮次**：属于租户数据，我没有删除。
  新锁只防止新增，**建议你评估后手工清理**。
- 未做：长时间内存观察、连接数耗尽验证（此前记录过约 20 小时后会 `WinError 10055`）。

---

# 长跑观察（第七轮）— 关闭最后一个未验证项

方法：对运行中的 API 施加**持续负载 180 秒**（~9 req/s，低于 600/60s 限流阈值，
以免测出来的是限流器而不是应用），同时每 10 秒采样一次进程 RSS / 连接数 / 线程数。
采样用受管环境的 `psutil`（项目 venv 不装 psutil，也不该为一个测量污染它）。

**先确认负载真的在跑**：脚本每 30 秒打印进度——第一次尝试因故 7 秒就退出，
若不做这个确认，"内存平稳"会被误读成"没有泄漏"，而实际上只是没跑。

## 结果

负载：**1640 个请求 / 180 秒，全部 200**，p50 **16ms**、p95 **23ms**、max **65ms**

| 时刻 | RSS | 连接数 | 线程 |
|---|---|---|---|
| 0s | 117.6MB | 8 | 6 |
| 90s | 117.9MB | 8 | 6 |
| 190s | 118.1MB | 6 | 5 |

**RSS 增长 +0.5MB，连接数不累积，线程数还回落。未发现短期泄漏迹象。**

## 这个结论的边界（重要）

**200 秒的观察不能证明"没有泄漏"**。项目既有记录里，约 20 小时的长跑会出现
`WinError 10055`（socket 耗尽）——那是一个**小时级**现象，本次观察窗口远短于它。
本轮的结论严格限定为：**在 3 分钟、1640 次请求下，没有可见的增长趋势**。

要覆盖小时级问题，需要的是更长的观察窗口（并记录基线），而不是更大的瞬时压力。

---

# 写入类流程（第八轮）— 补上只读走查看不到的部分

前面几轮都在读页面；这一轮驱动真实的**写入**路径。

## 通过项

| 检查 | 结果 |
|---|---|
| 输入**坏令牌** | 被拒，且提示含**服务端原因 + trace id**（"the bearer token does not resolve to an active membership (trace …)"）|
| 坏令牌**不被保存** | `localStorage.b2b_token === null` ✓ |
| 输入好令牌 | 接受并保存 ✓ |
| **新建特性开关**（定义 → 出现在列表） | 通过 ✓ |
| 未知路由 | 渲染"页面不存在"，未崩溃 ✓ |

## F-10【中】重新启用开关可能瞬间恢复放量，却无任何确认

**发现过程**：我在走查时以为"启用开关"应当确认，实测**没有弹窗**（`FAIL`）。
查代码后确认**我的预期错了**——启用按钮直接调接口，而"设置放量"才弹确认。
对**新建的 0% 开关**这是对的：0% 放量下启用等于没有影响。

**但顺着这个前提查下去，发现一个真实缺口**：后端语义里 `enabled` 是**覆盖放量的 kill switch**
（`flag_service`：`if not row.enabled: return enabled=False`），所以**停用不会清零放量**。
于是——一个停在 100%（或任意非 0）放量的开关被停用后再启用，会**瞬间恢复那部分流量**，
而这是一次**没有任何确认的单击**。

**修复**：仅当"重新启用且 `rollout_percent > 0`"时弹确认；0% 时保持一键操作（不加噪音）。

**浏览器实测**：
- 0% 的新开关启用 → **不弹**确认 ✓（无害场景无噪音）
- 仍带 50% 放量的开关重新启用 → **弹确认**，文案："该开关的放量仍为 50%，停用并不会清零。现在启用会立即恢复这部分流量。" ✓

**未验证**：确认框里点"取消"应保持停用——我的脚本在这一步写错了（在 promise 上调 `innerText`），
**没有跑成，因此不能声称验证过**。

## 顺带

- 我在这轮里**自己误报了一次**（把"启用不弹确认"当成 bug）。查证后是设计合理，只是我的预期错了。
  记录在此，因为**误报和漏报一样需要说明**。

---

# 写操作审批链路（第九轮）— 平台核心安全主张的实测

审批是"AI 可以提议、无权批准"落地的地方。此前我只见过它的**空状态**。
这一轮造出真实提议，把整条链路走了一遍。

## 链路实测（HTTP）

| 步骤 | 结果 |
|---|---|
| 提议 `case.eq_confirm`（`human_approval`） | `status=authorized`，`required_confirmation=true` ✓ |
| **未确认就执行** | **`CONFIRMATION_REQUIRED`** ✓ ← **核心安全属性成立** |
| 确认 | `status=confirmed` ✓ |
| 重复确认 | `PROPOSAL_NOT_CONFIRMABLE: confirmed` ✓（状态机正确）|

**值得记下的设计**：`confirm` 端点签名是 `(request, proposal_id)`，**不接收请求体**——
确认绑定的是**服务端存储的** `action_hash`，不信任客户端传来的哈希。
我一开始传了个错哈希并以为被接受了，读签名后确认**它根本不读这个字段**，这是更安全的做法。

## 页面实测

| 检查 | 结果 |
|---|---|
| 待批准计数 | 从 **0 变为 1** ✓ |
| 列表信息 | `case.eq_confirm` / **"需租户所有者批准"** / 待批准 / **"剩余 15 分钟"** ✓ |

页面把评审者做决定需要的东西都摆出来了：**要谁批、什么状态、还有多久过期**。
（唯一 FAIL 是我的选择器——列表不是 `<table>`；`innerText` 已证明该行存在。）

## ⚠ 本轮我误报了三次（这是过程问题，必须记）

1. "启用开关应该弹确认" → 实际启用直接调接口、"设置放量"才确认（设计合理）
2. "确认时传错哈希被接受" → 端点**不接收**请求体，绑定服务端哈希（更安全）
3. "有提议但列表为空" → 页面默认筛选是"待批准"，而我已用 API 把它确认成 `confirmed`（行为正确）

**三次都是同一个错误：没先读代码或状态就下判断。** 每次读一下就澄清了。
值得记下来，因为**误报和漏报一样会消耗信任**——区别只在于我每次都查证了才写进报告，
所以没有把假问题交出去。**结论：走查里"猜测 API 形状"是噪音来源，先读签名/状态是廉价的。**

---

# 补一条可复用的守卫：`scripts/ui_smoke.cjs`

## 为什么值得单独做

**审计里 5 个问题（F-5/F-7/F-8/F-9/F-10）都是 1741 条测试全绿也抓不到的**，而它们集中在同一处：
**接线**。F-5 最典型——工作台从 `{items,total}` 里读 `data.cases`，
于是库里有 21 条工单、界面显示"暂无工单"，而所有测试都是绿的：
`apiGet<T>` 的泛型是**断言不是校验**，且**没有任何测试打开过这个页面**。

所以我写了一个**零新依赖**的冒烟检查（`playwright-core` 与 Chromium 本机已有），
它按页面用**真实行选择器**核对：**接口返回了 N 行，页面是否真的渲染了 N 行**。

## 我在这条守卫上错了两次，都修掉了

1. **第一版用"空态关键词"匹配页面文本** → 把 `/gaps`（默认筛选"待处理"，而我早前造过草稿）
   和 `/workbench`（文本里恰好含关键词）都误报成空。**改用真实行选择器**。
2. **第二版 `served` 恒为 0** → 我比较的是 `/v1/cases`，而记录的 URL 带 `/api` 前缀，
   于是断言 `served > 0` **永远不成立**——**一个不会失败的守卫**。修掉前缀匹配后
   `served == rendered` 全部对上（23/23、1/1、8/8、2/2、1/1）。

## 变异测试：证明它真的会失败

把 F-5 那个 bug 临时改回去（`data?.items` → `data?.cases`），重跑：

```
FAIL /workbench (坐席工作台): /v1/cases returned 23 row(s) and the page rendered 0 (.workbench-item)
SUMMARY 9/10 routes consistent
```

恢复后 **10/10，退出码 0**。

**这一步是这条守卫唯一有价值的证据**：它精确地失败在它要防的那类 bug 上，
而不是"跑起来是绿的"——后者对守卫毫无意义。

## 用法

```bash
APP_BASE_URL=http://localhost:5173 \
APP_TOKEN=pt_<tenant-slug>_<user-id> \
node scripts/ui_smoke.cjs
```

需要 API 与前端在跑（这类缺陷只在那里出现）。退出码 0/1，可直接进 CI 或发布前检查清单。

---

# 提交安全性的自查（含一个我自己的失误）

## 我在本轮已经犯过一次"提交混入他人改动"的错

用户问"提交的文件能保证没问题吗"。回答之前我逐文件核对归属，**发现自己在功能阶段已经踩过一次**：

`1924f4b`（我的文档提交）把另一会话在 `docs/api-contracts.md` 里的**未提交小节**
`## Case evidence attachments` 一起提交了。证据：该小节在父提交 `de1a9ef` 中**不存在**（0 处），
在我的提交中存在（1 处）。

**这正是我明确说过要避免的事**（"不要把携带另一会话未提交改动的文件 add 进来"）。
当时的检查是"这个文件的 diff 里只有我要改的那几行吗"——我看了内容，却没意识到
**同一文件里还有别人的未提交段落**。

**影响**：HEAD 的 `docs/api-contracts.md` 描述了 `/v1/cases/{id}/attachments` 两个端点，
而它们的实现（`cases/attachments.py`、迁移 0040）**仍未提交** → **HEAD 中文档与代码不一致**。
严重性：低（仅文档漂移），且**当另一会话提交他们的代码后会自愈**。

**污染范围**：把"会话开始时已修改的文件清单"与"我本轮所有提交触及的文件"求交集，
**只有 `docs/api-contracts.md` 一个**，其余无污染。

**修法（未执行，等确认）**：该提交**尚未 push**，可以安全重写——
`git reset --soft HEAD~1` 后只重新 add 我写的三节。但**这是共享工作树里的历史重写**，
且另一会话可能正在提交，**我不擅自动手**。

## 结论：不能保证

**不能。** 具体分层：

| 范围 | 我的把握 |
|---|---|
| 我本轮的审计修复（未提交） | 每条都跑了测试 + 真实环境验证；F-3/F-9/F-10 有专门验证 |
| 另一会话的未提交改动 | **我没有审阅过**（attachments / intent / knowledge / docs） |
| "测试全绿"的含义 | **不等于没问题**——本轮 5 个 bug 就是全绿时存在的 |

**已知未验证项**（我在报告里都写明了）：
- F-10 确认框点"取消"应保持停用（我的脚本写错，没跑成）
- F-2 的生产默认值 45 秒这个**时长本身**未实测（只验证了分支逻辑）
- 小时级资源问题（`WinError 10055`）未覆盖

## F-10 的取消路径（已补验）

上一轮我把这条标为"未验证"（脚本在 promise 上调 `innerText`）。重测 **5/5 通过**：

| 检查 | 结果 |
|---|---|
| 启用后状态 | 已启用 ✓ |
| 停用后状态 | 已停用 ✓ |
| 重新启用时弹确认 | ✓ |
| 点"取消"后对话框关闭 | ✓ |
| **点"取消"后开关保持关闭** | ✓（状态"已停用"、按钮"启用"）|

**过程中我的断言又错了一次**：用 `/停用/` 判断"是否仍关闭"，但该行**同时**含状态"已停用"
与按钮"启用"两个字符串 → 一个**正确关闭**的行被判为失败。改为匹配状态词（`已停用` 存在且
`已启用` 不存在）后通过。**这是本轮第 4 次"我的检查本身出错"**——记录在此，因为它和第 3 轮那
三次误报是同一个根因：**没有先把被检查对象的实际形态看清楚**。

---

# 网络失败的真实 UX（第十轮）— 发现 U-4

用户原始要求里有"网络失败或超时"，此前我**只读了代码**（客户端的 20s 超时与错误映射），
从未实测。这轮把 API **真的停掉**，让 Vite 代理返回 500，再打开三个页面。

## U-4【中】代理形态下，宕机显示为一句"HTTP 500"

**实测**：API 停止后，`/cases`、`/quality`、`/workbench` 三个页面都显示 **"HTTP 500 重试"**。

**为什么这是问题**：运维看到 "HTTP 500" 会认为**服务端有 bug**，而真相是**服务端根本不在**。
两者该采取的行动完全不同（一个要查日志，一个要拉起进程）。

**根因（查到底了）**：客户端里那句好用的 "Could not reach the control plane" 只在
**浏览器自己连不上**（`TypeError`）时触发。**只要前面有代理或 ingress（也就是正常部署形态），
浏览器拿到的是 HTTP 响应，那条分支永远走不到。** 而 Vite 代理在目标不可达时返回的是
**500 + `text/plain` + 空 body** —— 于是：
- `if (text)` 分支被跳过（body 为空）
- 我第一版把修复加在"JSON 解析失败"的 catch 里 → **根本不会执行**（这也是我第一次改完没生效的原因）

**修复**：在 `toApiError` 的兜底处——**5xx 且服务端没给任何解释**时，明说"很可能宕机或重启中，稍后重试"。

**验证**：三个页面均显示
`The control plane answered HTTP 500 with no explanation, so it is most likely down or restarting. Retry in a moment.` + 重试 ✓

**遗留**：这条消息是英文，而界面是中文（客户端既有的 `TIMEOUT`/`NETWORK` 消息也全是英文）。
**没有**顺手改——客户端错误文案的本地化是另一件事，混进来只会让改动难以审阅。

## 我这轮的检查又太宽松了（第 5 次）

我的断言把 **`HTTP 5\d\d` 也算作"可操作的提示"**，所以第一版**给了 PASS**——
但它其实是这个问题的本体。**我把"存在一句话"当成了"这句话有用"**。
修好断言口径后（要求提到"连不上/宕机"）才算真的验证。这和第 3 轮那三次误报是同一根因：
**没有先想清楚"通过"的标准是什么**。
