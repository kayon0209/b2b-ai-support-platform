# 交接 — 华秋智能客服（2026-09-20 夜 → 09-21 凌晨）

给接手会话：**这份是自包含的。** 本次会话基于 `HANDOVER-2026-09-20.md`，
按用户给的《华秋智能客服-功能清单》逐项核对（**实测，不凭文档**）：
已实现跳过、冲突取更优 UX、缺失的补上。

**当前状态：26 个新提交，全部未 push（origin 共领先 46 个，其中 20 个是本次会话之前就未推送的）。**

---

## 1. 这一晚做了什么（17 个提交，按主题）

| # | 提交 | 对应清单 | 要点 |
|---|---|---|---|
| 1 | 投诉 L6 转人工 | §2.1 L6 / 6.4 | `agent_runtime/complaint.py`；**不按 Scene.COMPLAINT 判**（实测该词表含 `still not`/`third time`，会误伤订单问题、又漏掉报告自己举的例子）|
| 2 | contact→账户绑定 | 2.1 / 难点5 | 迁移 0041；**不用 inbox**（inbox 是渠道不是客户，且 29 条事件里 27 条 `inbox_id` 是字符串 `"None"`）|
| 3 | 绑定端点 HTTP 测试 | — | 补测试时发现真缺陷：重复绑定返回 **500 + 驱动报错**，已改 409 |
| 4 | 风控闸门成真控制 | 6.1/6.2 | 承诺拦截原挂在默认关闭的 flag 上且**零测试** → 改为无条件；并修漏检（宾语表只认 `delivery date`）|
| 5 | 坐席工作台 | 7.4/7.6 | `GET /v1/cases/{id}/workbench`；三个界面之一，此前完全缺失 |
| 6 | 漏点分析 | 8.1/8.2 | `handoff_reason_counts` + `automation_candidates`，**`automatable` 字段是重点**（证据缺口可自动化；策略性转人工不可，否则等于建议撤销控制）|
| 7 | 候选项关联真实问题 | 8.1 | 候选从"NO_AUTHORIZED_EVIDENCE × 42"变成带题目的工作队列 |
| 8 | 工具回执发布 | 4A.3/4A.4 | 此前**从不写 TOOL turn** → 客户只见 AI 转述；现回执进会话，卡片渲染 |
| 9 | 非工作时间兜底 | 7.5 | 不许假装有人；不说"多久" |
| 10 | 影子模式 | 9.2 | 全流程跑、不发送；run 保持 COMPLETED（扣发不是失败）|
| 11 | 技能组路由 | 7.3 | 场景→团队，一张表；**只推荐不指派**；未映射走通用队列不猜 |
| 12 | 身份归一可见性 | 2.1 | workbench 返回该账户全部 contact |
| 13 | 渠道记录 | 2.1 | 迁移 0042 `channel`；UI 显示 `channel: id` |
| 14 | 读路径端到端测试 | — | 补自己留的坑：回执发布此前只测了发布器，读路径从未被触达 |
| 15 | off-hours 默认修复 | — | **修我自己引入的回归**（见第 3 节）|
| 16 | 配置链路验证 | — | 此前所有 hours 用例都显式传参，配置项有没有被读到从未验证 |
| 17 | 报价引擎 | 4B | `platform_core/pricing/`；**不发布任何价目表** |
| 18 | 附件感知 | 1.3 | 只存内容类型（不存 URL/文件名/字节，Gerber 属客户 IP）；转人工便笺写明客户已附证据 |
| 19 | 外部系统故障话术 | 10.2 | 系统挂了就说系统挂了；**不谎称已建工单**、不承诺时间 |
| 20 | 交接文档 | — | 本文件 |
| 21 | 参考价目表 | 4B | 取自公开数据（PCBSync 2026-08-20，USD/FOB Shenzhen）；**带出处与 NON-CONTRACTUAL 警告** |
| 22 | 人工修正回流 | 7.8 | 迁移 0043 `answer_corrections`；记录/审核/批准，**绝不自动学习** |
| 23 | 本地演示 ERP | 10.1 | `business_api` provider，样例数据，记录带 `source: demo` |
| 24 | 报价分支运行级验证 | 4B | 跑真实 run 断言 `quote_band=` 落到坐席便笺（防"测了函数却没人调"）|
| 25 | 待审核修正进看板 | 7.8 | `/v1/quality/metrics` 增 `pending_corrections`，看板卡片 |
| 26 | 交接文档同步 | — | 本文件 |

---

## 2. 实测发现（比代码本身更有价值的部分）

- **`inbox_id` 在 29 条事件里 27 条是字符串 `"None"`**（`minimize.py` 对缺失字段做 `str(...)` 的结果）→ 上一版"按 inbox 建关联"的前提不成立。
- **`contact_id` 0/29、`sender_id` 1/29** → contact 身份只能从 Chatwoot API 取（`fetch_message_contact_id`，取 `sender.id` 且 `type=="contact"`）。
- 同一条外部事实卡住两处：`minimize` 的 contact 提取 **和** `_persist_memory` 的"按 contact 存长期事实"，**当前部署下从没被喂过数据**。
- **`conversation_turns.source` 是 VARCHAR(15)**，工具名放不进去 → 放进 payload。
- **turn 写入走 PII 脱敏**：10 位 epoch 会被打成 `[PHONE]`，**回执变成非法 JSON**。对策：脱敏后存活不了的回执**一律不发布**（并记录日志）。
- **全仓库此前没有任何测试启用过 `agent.business_read_enabled`** → plan 3.2 整条读路径无人触达。

---

## 3. 我自己引入的回归（全量当晚抓到）

7.5 那次提交，我在提交信息里写"未配置 = 一直开门"，代码却默认了 **9–18 这个已配置窗口**
→ 18:00 之后所有转人工话术变成离线口径 → 3 条无关测试挂掉。

已改为从 `support_open_hour` / `support_close_hour` 读取（默认 `0/0` = 未配置 = 一直开门），
并加 `test_the_default_is_unconfigured_and_therefore_always_open` 防复发。

**教训：默认值就是行为变更，而且它会挑时间（下班后）才暴露。时间相关的默认值必须在一天里不同时段各跑一次全量。**

---

## 4. 未提交的文件（**别整体 commit**）

工作区里这些不是我的，是上一轮/另一会话的工作，**一行没动**：
`0040_case_attachments.py`、`cases/attachments.py`、`test_case_attachments.py`、
`intent.py`、`cases/models.py`、`knowledge/*`、`docs/*`、`HANDOVER-2026-09-19.md`、`vite.config.ts`。

注意：`docs/research/huaqiu-research.md` 里有我新增的**附录 K / L**（未提交）。

---

## 5. 剩余（都卡在外部事实或较大 schema 决策，不是能凭空补的）

- **10.1 真实 ERP 适配器**（已完成）：无外部系统 → 本地演示 ERP（`business_api` provider，
  样例数据，`source: demo`）。接真实系统时把 `business_api_adapter` 改为 `http` 即可，无需改调用点。
- **7.8 人工修正回流**（已完成）：迁移 0043 + 审核流程 + 看板可见。**真正的下一步**：
  "已批准"的修正 → 自动写入知识库（draft→publish）目前仍是人工一步。
  注意别偷偷扩 `GAP_REASON_CODES`（有测试钉住了哪些码不算 gap）。
- **4B 真实价目表与制程矩阵**：引擎已就位，缺华秋的报价规则与六个基地能力——**必须来自业务方，不能替他们编**（编数字正是"无据声明"）。
- **2.1 的跨渠道身份**（同一个人多 contact）与 **7.3 的租户级团队配置**：都需 schema 决策。
- 建议单开一条：**回执走 PII 脱敏会损坏结构化 JSON**，彻底修需给 turn 加结构化列（迁移）。

---

## 6. 验证（照抄）

```bash
cd "D:/360Downloads/360驱动大师目录/b2b-ai-support-plan/b2b-ai-support-plan"
R="$(cygpath -w "$(pwd)")"
export PYTHONPATH="$R/apps/api/src;$R/packages/contracts/src;$R/packages/policy/src;$R/packages/observability/src;$R/apps/worker/src"

./.venv/Scripts/python.exe -m ruff check apps packages scripts tests pytest_plugins_release
./.venv/Scripts/python.exe -m ruff format --check apps packages scripts tests pytest_plugins_release
./.venv/Scripts/python.exe -m mypy
mv tests/artifacts/release_gate_evidence.json /tmp/   # 沙箱守卫
./.venv/Scripts/python.exe -m pytest --junitxml=tests/artifacts/junit-final.xml
./.venv/Scripts/python.exe -m platform_core.evaluation.release_check --evidence-only
```

前端：`cd apps/admin-web && node ./node_modules/typescript/bin/tsc --noEmit && node ./node_modules/vite/bin/vite.js build`

**最后一次全量：1709 收集 / 0 失败 / 0 错误 / 0 跳过；release_check exit 0（15/4/3）。**
EOF
echo "written"; wc -l HANDOVER-2026-09-21.md